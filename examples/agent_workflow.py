"""
Phase 2 — GovAgent multi-step workflows (planned).

When `GovAgent` lands in `nepal_gov_agent.agent`, this example will show
supervisor/pipeline patterns over the same corpus. See README roadmap.
"""

if __name__ == "__main__":
    print("GovAgent workflows — Phase 2 (see README roadmap).")
